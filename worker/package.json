{
  "name": "emandeval-chat-worker",
  "version": "1.1.0",
  "private": true,
  "description": "Cloudflare Worker backend for the eMANDEVAL Future Policy Assistant. Calls the Gemini API server side and keeps the API key as a secret.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
